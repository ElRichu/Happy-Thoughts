<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/greyBar</item>
        <item name="colorPrimaryDark">@color/greyBar</item>
        <item name="colorAccent">@color/greyBar</item>
    </style>

</resources>
