<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="h4213.smart">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="n">
        <activity android:name=".activities.AlarmSettingActivity"/>
        <receiver android:name=".services.AlarmReceiver" />
        <activity android:name=".activities.HelpPopUpActivity" />
        <activity android:name=".activities.AccountActivity" />
        <service android:name=".services.AlarmService" />
        <activity android:name=".activities.CredentialsSettingsActivity" />
        <activity android:name=".activities.SettingsActivity" />
        <activity android:name=".services.GoogleSignUp" />
        <activity android:name=".services.GoogleLogin" />
        <activity android:name=".activities.QuestionnaireActivity" />
        <activity android:name=".activities.HomepageActivity" />
        <activity android:name=".activities.LanguagesActivity" />
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.AuthenticationActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>